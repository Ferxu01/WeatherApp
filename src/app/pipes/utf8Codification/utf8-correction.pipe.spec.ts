import { Utf8CorrectionPipe } from './utf8-correction.pipe';

describe('Utf8CorrectionPipe', () => {
  it('create an instance', () => {
    const pipe = new Utf8CorrectionPipe();
    expect(pipe).toBeTruthy();
  });
});
